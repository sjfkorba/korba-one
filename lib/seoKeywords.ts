export const generateSeoKeywords = (subcategoryLabel: string) => {
  const city = "Korba";

  return [
    `Best ${subcategoryLabel} in ${city}`,
    `${city} ${subcategoryLabel}`,
    `Top ${subcategoryLabel} near me`,
    `${subcategoryLabel} services in ${city}`,
    `Affordable ${subcategoryLabel} in ${city}`,
    `${city} verified ${subcategoryLabel}`,
    `Trusted ${subcategoryLabel} in ${city}`,
    `${subcategoryLabel} contact number ${city}`,
    `${city} ${subcategoryLabel} price`,
    `${subcategoryLabel} listing in ${city}`,
    `Nearby ${subcategoryLabel} ${city}`,
    `${subcategoryLabel} business ${city}`,
    `Professional ${subcategoryLabel} ${city}`,
    `${city} local ${subcategoryLabel}`,
    `Premium ${subcategoryLabel} in ${city}`
  ];
};
